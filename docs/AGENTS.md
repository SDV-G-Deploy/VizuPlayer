# AGENTS (Docs Scope)

This file is a helper mirror for docs folder context.
The canonical policy source is root `AGENTS.md`.

## Working context

- Local workspace: `C:\ForCodexAnd\VizuPlayer`
- Remote repo: `https://github.com/SDV-G-Deploy/VizuPlayer`
- Repository role: `Core / Engine / Integration Demo`

## Required workflow rules

1. Implement all changes locally first.
2. Push to GitHub only after local checks pass.
3. Use explicit pass target (`Core demo improvement` or `App/product development`).
4. Do not silently mix Core/App tracks in one default pass.
5. Before broad UX/UI or visual work, explicitly confirm target track.
6. Sync policy updates with root `AGENTS.md` first.

## Documentation update targets

- `docs/PROJECT_STATE.md`
- `docs/SESSION_LOG.md`
- `docs/CHANGELOG_AGENT.md`
- Root planning/governance docs when project direction changed
